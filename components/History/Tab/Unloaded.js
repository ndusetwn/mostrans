import React, { Component } from "react";
import { Content, Card, CardItem, Text, Body } from "native-base";

export default class Unloaded extends Component {
  render() {
    return (
      <Content padder>
        <Text>Unloaded</Text>
      </Content>
    );
  }
}